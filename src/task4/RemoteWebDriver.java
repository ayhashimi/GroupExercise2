package task4;

public interface RemoteWebDriver extends WebDriver, TakeScreenshot
{
    void navigate();  //method-stub  - all methods have return types,
                      // all methods have a name,
                      // all methods have a set of parameters (even if those parameters are nothing at all)
}
